import pandas as pd

def enrich_product_info(df: pd.DataFrame, product_path="data/lista_articulos_clean.csv") -> pd.DataFrame:
    """Placeholder for product enrichment logic."""
    print("ℹ️ Product enrichment not applied (minimal mode).")
    return df
