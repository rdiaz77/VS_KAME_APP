import pandas as pd

def format_numeric_columns(df: pd.DataFrame, for_preview=False) -> pd.DataFrame:
    """Placeholder for later numeric formatting logic."""
    print("ℹ️ Numeric formatting not applied (minimal mode).")
    return df
